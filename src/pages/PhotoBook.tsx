import styled from "styled-components";

function PhotoBook() {
  return <PhotoBookPage />;
}

export default PhotoBook;

const PhotoBookPage = styled.div`
  position: fixed;
  inset: 0;
  background-color: #ffcde2;
`;
